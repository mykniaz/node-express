<div class="row">
	<div class="col s12">
		<h1>Orders Page</h1>
	</div>
</div>
<div class="row">
	<div class="col s12">
		{{#if orders.length}}
			{{#each order}}
				<div>Order</div>
			{{/each}}
		{{else}}
			<p>No orders is available</p>
		{{/if}}
	</div>
</div>
